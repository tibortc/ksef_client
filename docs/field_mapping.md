<!-- GENERATED by `rake fa3:field_mapping` from FA(3) 1-0E — DO NOT EDIT. -->

# FA(3) field mapping

Every field this gem's model carries, and the FA(3) element it reads and writes.

**Generated**, not written: the attribute names come from the model classes, and the
element names, types, cardinalities and descriptions from the pinned XSD. Editing this
file by hand will be overwritten — change `tasks/field_mapping.rb` instead, and note
that a declared element which does not exist in the schema fails the build rather than
appearing here (DESIGN.md §7.2).

Descriptions are the Ministry's own `xsd:documentation`, verbatim and in Polish, and
are shown only where every occurrence of that element name in the schema agrees.
**Field-name truth is the XSD**, not this table.

This model does not carry all of FA(3). `Ksef::FA3::Invoice#unmapped_elements` reports,
for a parsed document, exactly which element paths `#to_xml` would drop — computed by
difference against the serializer, so it cannot go stale.


## Invoice

`Ksef::FA3::Invoice` — The document itself. `Faktura` in the schema; its scalar fields live under `Fa`, its parties directly under the root.

| Attribute | FA(3) element | Type | Occurs | Ministry's description |
|---|---|---|---|---|
| `number` | `P_2` | `TZnakowy` | 1 | Kolejny numer faktury, nadany w ramach jednej lub więcej serii, który w sposób jednoznaczny identyfikuje fakturę |
| `issue_date` | `P_1` | `TDataT` | 1 | Data wystawienia, z zastrzeżeniem art. 106na ust. 1 ustawy |
| `currency` | `KodWaluty` | `TKodWaluty` | 1 | Kod waluty (ISO 4217) |
| `invoice_type` | `RodzajFaktury` | `TRodzajFaktury` | 1 | Rodzaj faktury |
| `issued_at` | `DataWytworzeniaFa` | *(inline)* | 1 | Data i czas wytworzenia faktury |
| `seller` | `Podmiot1` | *(inline)* | 1 | Dane podatnika. Imię i nazwisko lub nazwa sprzedawcy towarów lub usług |
| `buyer` | `Podmiot2` | *(inline)* | 1 | Dane nabywcy |
| `lines` | `FaWiersz` | *(inline)* | 0–10000 | Szczegółowe pozycje faktury w walucie, w której wystawiono fakturę - węzeł opcjonalny dla faktury zaliczkowej, faktury korygującej fakturę zaliczkową oraz faktur korygujących dotyczących wszystkich dostaw towarów lub usł […] |
| `annotations` | `Adnotacje` | *(inline)* | 1 | Inne adnotacje na fakturze |
| `correction` | `DaneFaKorygowanej` | *(inline)* | 1–50000 | Dane faktury korygowanej |
| `totals` | `P_15` | `TKwotowy` | 1 | Kwota należności ogółem. W przypadku faktur zaliczkowych - kwota zapłaty dokumentowana fakturą. W przypadku faktur, o których mowa w art. 106f ust. 3 ustawy - kwota pozostała do zapłaty. […] |
| `order` | `Zamowienie` | *(inline)* | 0–1 | Zamówienie lub umowa, o których mowa w art. 106f ust. 1 pkt 4 ustawy (dla faktur zaliczkowych), w walucie, w której wystawiono fakturę zaliczkową. […] |
| `advances` | `FakturaZaliczkowa` | *(inline)* | 0–100 | Numery faktur zaliczkowych lub ich numery KSeF, jeśli zostały wystawione z użyciem KSeF |
| `rounding` | — | — | — | **Not an element.** Not in the document at all. Which rounding strategy produced the summaries is inferred on parse (`RoundingInference`), never stated. |
| `raw_document` | — | — | — | **Not an element.** Provenance, not content — the retained source document, excluded from identity (§8.2b). |
| `stated_gross` | — | — | — | **Not an element.** Carries `Fa/P_15` when it differs from the derived total; it is the same element as `#gross_total` writes, not a second one (§17.2). |

## Subject — a party

`Ksef::FA3::Subject` — One party. Written as `Podmiot1` (seller), `Podmiot2` (buyer), or their `K` twins `Podmiot1K`/`Podmiot2K` on a correction, which state the parties as the corrected invoice had them. Paths below use `Podmiot2`.

| Attribute | FA(3) element | Type | Occurs | Ministry's description |
|---|---|---|---|---|
| `nip` | `NIP` | `etd:TNrNIP` | 1 | Identyfikator podatkowy NIP |
| `name` | `Nazwa` | `TZnakowy512` | 1 | Imię i nazwisko lub nazwa |
| `address` | `Adres` | `TAdres` | 0–1 | — |
| `local_government_unit` | `JST` | *(inline)* | 1 | Znacznik jednostki podrzędnej JST. Wartość "1" oznacza, że faktura dotyczy jednostki podrzędnej JST. W takim przypadku, aby udostępnić fakturę jednostce podrzędnej JST, należy wypełnić sekcję Podmiot3, w szczególności po […] |
| `vat_group_member` | `GV` | *(inline)* | 1 | Znacznik członka grupy VAT. Wartość "1" oznacza, że faktura dotyczy członka grupy VAT. W takim przypadku, aby udostępnić fakturę członkowi grupy VAT, należy wypełnić sekcję Podmiot3, w szczególności podać NIP lub ID-Wew […] |
| `buyer_id` | — | — | — | **Not an element.** `Podmiot2/IDNabywcy` and `Podmiot2K/IDNabywcy` only; a seller has no such element. |

## Address

`Ksef::FA3::Address` — FA(3) has no street/city/postcode fields: it takes two free-text lines. `Address` composes them at construction and keeps the composed form (§8.2b), so `street`/`city`/`postal_code` are constructor sugar rather than attributes.

| Attribute | FA(3) element | Type | Occurs | Ministry's description |
|---|---|---|---|---|
| `line1` | `AdresL1` | `TZnakowy512` | 1 | Adres [Address] |
| `line2` | `AdresL2` | `TZnakowy512` | 0–1 | Adres [Address] |
| `country` | `KodKraju` | `etd:TKodKraju` | 1 | — |

## Line — an invoice row

`Ksef::FA3::Line` — One `FaWiersz`. Every child but `NrWierszaFa` is optional, so most fields here may be absent from a legal document (§8.6).

| Attribute | FA(3) element | Type | Occurs | Ministry's description |
|---|---|---|---|---|
| `name` | `P_7` | `TZnakowy512` | 0–1 | Nazwa (rodzaj) towaru lub usługi. Pole opcjonalne wyłącznie dla przypadku określonego w art 106j ust. 3 pkt 2 ustawy (faktura korygująca) |
| `unit` | `P_8A` | `TZnakowy` | 0–1 | Miara dostarczonych towarów lub zakres wykonanych usług. Pole opcjonalne dla przypadku określonego w art. 106e ust. 5 pkt 3 ustawy |
| `quantity` | `P_8B` | `TIlosci` | 0–1 | Ilość (liczba) dostarczonych towarów lub zakres wykonanych usług. Pole opcjonalne dla przypadku określonego w art. 106e ust. 5 pkt 3 ustawy |
| `net_unit_price` | `P_9A` | `TKwotowy2` | 0–1 | Cena jednostkowa towaru lub usługi bez kwoty podatku (cena jednostkowa netto). Pole opcjonalne dla przypadków określonych w art. 106e ust. 2 i 3 oraz ust. 5 pkt 3 ustawy |
| `net_amount` | `P_11` | `TKwotowy` | 0–1 | Wartość dostarczonych towarów lub wykonanych usług, objętych transakcją, bez kwoty podatku (wartość sprzedaży netto). Pole opcjonalne dla przypadków określonych w art. 106e ust. 2 i 3 oraz ust. 5 pkt 3 ustawy |
| `vat_rate` | `P_12` | `TStawkaPodatku` | 0–1 | Stawka podatku. Pole opcjonalne dla przypadków określonych w art. 106e ust. 2, 3, ust. 4 pkt 3 i ust. 5 pkt 3 ustawy |
| `row_number` | `NrWierszaFa` | `TNaturalny` | 1 | Kolejny numer wiersza faktury |
| `state_before` | `StanPrzed` | `etd:TWybor1` | 0–1 | Znacznik stanu przed korektą w przypadku faktury korygującej lub faktury korygującej fakturę wystawioną w związku z art. 106f ust. […] |

## Totals — a stated summary

`Ksef::FA3::Totals` — What a document states rather than what its rows imply. Present on the six types in `Invoice::STATED_TOTALS_TYPES`; a `VAT` invoice derives its summary instead (§8.4, §8.5).

| Attribute | FA(3) element | Type | Occurs | Ministry's description |
|---|---|---|---|---|
| `gross` | `P_15` | `TKwotowy` | 1 | Kwota należności ogółem. W przypadku faktur zaliczkowych - kwota zapłaty dokumentowana fakturą. W przypadku faktur, o których mowa w art. 106f ust. 3 ustawy - kwota pozostała do zapłaty. […] |
| `buckets` | — | — | — | **Not an element.** A Hash keyed by element name — `P_13_1`, `P_14_1`, … — so each key *is* its element. See the summary-bucket table below. |

## Correction

`Ksef::FA3::Correction` — The group that makes a `KOR`, `KOR_ZAL` or `KOR_ROZ` a correction. Optional as a whole; `DaneFaKorygowanej` is mandatory once any of it is present (§8.4).

| Attribute | FA(3) element | Type | Occurs | Ministry's description |
|---|---|---|---|---|
| `reason` | `PrzyczynaKorekty` | `TZnakowy` | 0–1 | Przyczyna korekty dla faktur korygujących |
| `effect` | `TypKorekty` | `TTypKorekty` | 0–1 | Typ skutku korekty w ewidencji dla podatku od towarów i usług |
| `corrected` | `DaneFaKorygowanej` | *(inline)* | 1–50000 | Dane faktury korygowanej |
| `period` | `OkresFaKorygowanej` | `TZnakowy` | 0–1 | Dla faktury korygującej, o której mowa w art. 106j ust. 3 ustawy - okres, do którego odnosi się udzielany opust lub udzielana obniżka, w przypadku gdy podatnik udziela opustu lub obniżki ceny w odniesieniu do dostaw towa […] |
| `corrected_number` | `NrFaKorygowany` | `TZnakowy` | 0–1 | Poprawny numer faktury korygowanej w przypadku, gdy przyczyną korekty jest błędny numer faktury korygowanej. W takim przypadku błędny numer faktury należy wskazać w polu NrFaKorygowanej |
| `previous_seller` | `Podmiot1K` | *(inline)* | 0–1 | W przypadku korekty danych sprzedawcy należy podać pełne dane sprzedawcy występujące na fakturze korygowanej. Pole nie dotyczy przypadku korekty błędnego NIP występującego na fakturze pierwotnej - wówczas wymagana jest k […] |
| `previous_buyers` | `Podmiot2K` | *(inline)* | 0–101 | W przypadku korekty danych nabywcy występującego jako Podmiot2 lub dodatkowego nabywcy występującego jako Podmiot3 należy podać pełne dane tego podmiotu występujące na fakturze korygowanej. […] |
| `paid_before` | `P_15ZK` | `TKwotowy` | 1 | W przypadku korekt faktur zaliczkowych - kwota zapłaty przed korektą. W przypadku korekt faktur, o których mowa w art. 106f ust. 3 ustawy - kwota pozostała do zapłaty przed korektą |
| `exchange_rate_before` | `KursWalutyZK` | `TIlosci` | 0–1 | Kurs waluty stosowany do wyliczenia kwoty podatku w przypadkach, o których mowa w dziale VI ustawy przed korektą |

## CorrectedInvoice — which invoice is corrected

`Ksef::FA3::CorrectedInvoice` — One `DaneFaKorygowanej`. Its choice group names the corrected invoice either by KSeF number or by number-and-date, and the branches are not symmetrical with `FakturaZaliczkowa`'s (§8.5).

| Attribute | FA(3) element | Type | Occurs | Ministry's description |
|---|---|---|---|---|
| `number` | `NrFaKorygowanej` | `TZnakowy` | 1 | Numer faktury korygowanej |
| `issue_date` | `DataWystFaKorygowanej` | `TDataT` | 1 | Data wystawienia faktury korygowanej |
| `ksef_number` | `NrKSeFFaKorygowanej` | `TNumerKSeF` | 1 | Numer identyfikujący fakturę korygowaną w KSeF |

## Order — an advance invoice's order

`Ksef::FA3::Order` — `Zamowienie`, which replaces `FaWiersz` entirely on a `ZAL`. `WartoscZamowienia` is the whole order **including tax** (§8.5).

| Attribute | FA(3) element | Type | Occurs | Ministry's description |
|---|---|---|---|---|
| `total` | `WartoscZamowienia` | `TKwotowy` | 1 | Wartość zamówienia lub umowy z uwzględnieniem kwoty podatku |
| `lines` | `ZamowienieWiersz` | *(inline)* | 1–10000 | Szczegółowe pozycje zamówienia lub umowy w walucie, w której wystawiono fakturę zaliczkową |

## OrderLine — an order position

`Ksef::FA3::OrderLine` — One `ZamowienieWiersz`. Unlike {Line} nothing here is derived: the document states both the net and the tax, so both are read (§8.5).

| Attribute | FA(3) element | Type | Occurs | Ministry's description |
|---|---|---|---|---|
| `name` | `P_7Z` | `TZnakowy512` | 0–1 | Nazwa (rodzaj) towaru lub usługi |
| `unit` | `P_8AZ` | `TZnakowy` | 0–1 | Miara zamówionego towaru lub zakres usługi |
| `quantity` | `P_8BZ` | `TIlosci` | 0–1 | Ilość zamówionego towaru lub zakres usługi |
| `net_unit_price` | `P_9AZ` | `TKwotowy2` | 0–1 | Cena jednostkowa netto |
| `net_amount` | `P_11NettoZ` | `TKwotowy` | 0–1 | Wartość zamówionego towaru lub usługi bez kwoty podatku |
| `vat_amount` | `P_11VatZ` | `TKwotowy` | 0–1 | Kwota podatku od zamówionego towaru lub usługi |
| `vat_rate` | `P_12Z` | `TStawkaPodatku` | 0–1 | Stawka podatku |
| `row_number` | `NrWierszaZam` | `TNaturalny` | 1 | Kolejny numer wiersza zamówienia lub umowy |
| `state_before` | `StanPrzedZ` | `etd:TWybor1` | 0–1 | Znacznik stanu przed korektą w przypadku faktury korygującej fakturę dokumentującą otrzymanie zapłaty lub jej części przed dokonaniem czynności oraz fakturę wystawioną w związku z art. 106f ust. […] |

## AdvanceInvoice — an advance already settled

`Ksef::FA3::AdvanceInvoice` — One `FakturaZaliczkowa` on a `ROZ` or `KOR_ROZ`. Its choice is **inverted** from `DaneFaKorygowanej`'s: the marker `NrKSeFZN` pairs with the plain number, and the KSeF branch is the number alone (§8.5).

| Attribute | FA(3) element | Type | Occurs | Ministry's description |
|---|---|---|---|---|
| `number` | `NrFaZaliczkowej` | `TZnakowy` | 1 | Numer faktury zaliczkowej wystawionej poza KSeF. Pole obowiązkowe dla faktury wystawianej po wydaniu towaru lub wykonaniu usługi, o której mowa w art. 106f ust. 3 ustawy i ostatniej z faktur, o której mowa w art. […] |
| `ksef_number` | `NrKSeFFaZaliczkowej` | `TNumerKSeF` | 1 | Numer identyfikujący fakturę zaliczkową w KSeF. Pole obowiązkowe w przypadku, gdy faktura zaliczkowa była wystawiona za pomocą KSeF |

## Summary buckets

`Totals#buckets` is keyed by element name. Which bucket a row lands in is decided by its `P_12` rate code, and **the map is not invertible** — several codes share one bucket (§8.1a). Three buckets are reachable by no rate code at all, which is a limit of this model rather than of FA(3).

| Element | Rate codes | Ministry's description |
|---|---|---|
| `P_13_1` | `23`, `22` | Suma wartości sprzedaży netto objętej stawką podstawową - aktualnie 23% albo 22%. W przypadku faktur zaliczkowych - wartość zaliczki netto. W przypadku faktur korygujących - kwota różnicy, o której mowa w art. 106j ust. […] |
| `P_14_1` | `23`, `22` | Kwota podatku od sumy wartości sprzedaży netto objętej stawką podstawową - aktualnie 23% albo 22%. W przypadku faktur zaliczkowych - kwota podatku wyliczona według wzoru, o którym mowa w art. 106f ust. 1 pkt 3 ustawy. […] |
| `P_13_2` | `8`, `7` | Suma wartości sprzedaży netto objętej stawką obniżoną pierwszą - aktualnie 8 % albo 7%. W przypadku faktur zaliczkowych - wartość zaliczki netto. W przypadku faktur korygujących - kwota różnicy, o której mowa w art. […] |
| `P_14_2` | `8`, `7` | Kwota podatku od sumy wartości sprzedaży netto objętej stawką obniżoną pierwszą - aktualnie 8% albo 7%. W przypadku faktur zaliczkowych - kwota podatku wyliczona według wzoru, o którym mowa w art. 106f ust. […] |
| `P_13_3` | `5` | Suma wartości sprzedaży netto objętej stawką obniżoną drugą - aktualnie 5%. W przypadku faktur zaliczkowych - wartość zaliczki netto. W przypadku faktur korygujących - kwota różnicy, o której mowa w art. 106j ust. […] |
| `P_14_3` | `5` | Kwota podatku od sumy wartości sprzedaży netto objętej stawką obniżoną drugą - aktualnie 5%. W przypadku faktur zaliczkowych - kwota podatku wyliczona według wzoru, o którym mowa w art. 106f ust. 1 pkt 3 ustawy. […] |
| `P_13_4` | `4`, `3` | Suma wartości sprzedaży netto objętej ryczałtem dla taksówek osobowych. W przypadku faktur zaliczkowych - wartość zaliczki netto. W przypadku faktur korygujących - kwota różnicy, o której mowa w art. 106j ust. […] |
| `P_14_4` | `4`, `3` | Kwota podatku od sumy wartości sprzedaży netto w przypadku ryczałtu dla taksówek osobowych. W przypadku faktur zaliczkowych - kwota podatku wyliczona według wzoru, o którym mowa w art. 106f ust. 1 pkt 3 ustawy. […] |
| `P_13_5` | *(no rate code reaches it)* | Suma wartości sprzedaży netto w przypadku procedury szczególnej, o której mowa w dziale XII w rozdziale 6a ustawy. W przypadku faktur zaliczkowych - wartość zaliczki netto. […] |
| `P_14_5` | *(no rate code reaches it)* | Kwota podatku od wartości dodanej w przypadku procedury szczególnej, o której mowa w dziale XII w rozdziale 6a ustawy. W przypadku faktur zaliczkowych - kwota podatku wyliczona według wzoru, o którym mowa w art. […] |
| `P_13_6_1` | `0 KR` | Suma wartości sprzedaży objętej stawką 0% z wyłączeniem wewnątrzwspólnotowej dostawy towarów i eksportu. W przypadku faktur zaliczkowych - wartość zaliczki. […] |
| `P_13_6_2` | `0 WDT` | Suma wartości sprzedaży objętej stawką 0% w przypadku wewnątrzwspólnotowej dostawy towarów. W przypadku faktur korygujących - kwota różnicy, o której mowa w art. 106j ust. 2 pkt 5 ustawy |
| `P_13_6_3` | `0 EX` | Suma wartości sprzedaży objętej stawką 0% w przypadku eksportu. W przypadku faktur zaliczkowych - wartość zaliczki. W przypadku faktur korygujących - kwota różnicy, o której mowa w art. 106j ust. 2 pkt 5 ustawy |
| `P_13_7` | `zw` | Suma wartości sprzedaży zwolnionej od podatku. W przypadku faktur zaliczkowych - wartość zaliczki. W przypadku faktur korygujących - kwota różnicy wartości sprzedaży |
| `P_13_8` | `np I` | Suma wartości sprzedaży w przypadku dostawy towarów oraz świadczenia usług poza terytorium kraju, z wyłączeniem kwot wykazanych w polach P_13_5 i P_13_9. W przypadku faktur zaliczkowych - wartość zaliczki. […] |
| `P_13_9` | `np II` | Suma wartości świadczenia usług, o których mowa w art. 100 ust. 1 pkt 4 ustawy. W przypadku faktur zaliczkowych - wartość zaliczki. W przypadku faktur korygujących - kwota różnicy wartości sprzedaży |
| `P_13_10` | `oo` | Suma wartości sprzedaży w procedurze odwrotnego obciążenia, dla której podatnikiem jest nabywca zgodnie z art. 17 ust. 1 pkt 7 i 8 ustawy oraz innych przypadków odwrotnego obciążenia występujących w obrocie krajowym. […] |
| `P_13_11` | *(no rate code reaches it)* | Suma wartości sprzedaży w procedurze marży, o której mowa w art. 119 i art. 120 ustawy. W przypadku faktur zaliczkowych - wartość zaliczki. W przypadku faktur korygujących - kwota różnicy wartości sprzedaży |

---

*Schema: `lib/ksef/fa3/schema/schemat_FA(3)_v1-0E.xsd`. Regenerate with `rake fa3:field_mapping`; `rake fa3:verify`
fails if this file is stale.*
